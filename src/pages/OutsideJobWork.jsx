import PlatingAnalytics from './PlatingAnalytics';

const OutsideJobWork = () => (
  <PlatingAnalytics
    jobWorkType="OUTSIDE"
    title="Outside job work"
    subtitle="Vendor-wise analytics for Out-Side job work — open a vendor to drill in"
    accent="blue"
  />
);

export default OutsideJobWork;
